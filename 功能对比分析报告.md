# EvoClaw vs OpenClaw 功能对比分析报告

> 对比日期：2026-05-23 | OpenClaw 版本：2026.5.22 | EvoClaw 版本：0.4.0

---

## 一、总体概述

| 维度 | OpenClaw | EvoClaw |
|------|----------|---------|
| 定位 | 个人 AI 助理（Product-first） | 自进化 AI 代理操作系统（Platform-first） |
| 架构 | 单体 + 插件扩展 | pnpm monorepo（15 子包） |
| 代码规模 | 51,468 commits, 1732 branches | 初始阶段 |
| 语言 | TypeScript (strict) | TypeScript |
| 构建工具 | tsc + tsdown | tsc |
| Lint/Format | oxlint + oxfmt | 无 |
| 测试框架 | Vitest + Playwright | 无 |
| CI/CD | GitHub Actions 全矩阵 | 无 |
| 包管理 | pnpm 11 | pnpm 10 |
| Node 版本 | >= 22.19+ | >= 22 |

---

## 二、核心架构对比

### 2.1 网关协议

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| WebSocket 协议 | JSON-RPC 风格请求/响应/事件，TypeBox Schema 验证 | 基础 WebSocket 支持 | **大** |
| REST API | 有限（Webhook/Hooks） | 完整 REST API（Express） | EvoClaw 更优 |
| MCP 协议 | 不支持 | 完整 MCP Gateway | **EvoClaw 独有** |
| Protocol 类型生成 | Swift 模型代码生成 | 无 | **大** |
| Device Pairing | 完整的设备身份 + challenge 签名机制 | JWT Token 认证 | **大** |
| 网关发现 | `config.schema.lookup` 动态 Schema | 无 | 缺失 |
| 配置热重载 | 文件监听 + 自动应用 | 无 | **大** |

### 2.2 多通道消息系统

| 通道 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| WhatsApp (Baileys) | 完整 | 无 | **缺失** |
| Telegram (grammY) | 完整 | 无 | **缺失** |
| Discord | 完整 | 无 | **缺失** |
| Slack | 完整 | 无 | **缺失** |
| Signal | 完整 | 无 | **缺失** |
| iMessage | 完整 | 无 | **缺失** |
| Google Chat | 完整 | 无 | **缺失** |
| Microsoft Teams | 完整 | 无 | **缺失** |
| Matrix | 完整 | 无 | **缺失** |
| Feishu (飞书) | 完整 | 无 | **缺失** |
| LINE | 完整 | 无 | **缺失** |
| WeChat (微信) | 完整 | 无 | **缺失** |
| QQ | 完整 | 无 | **缺失** |
| IRC | 完整 | 无 | **缺失** |
| Mattermost | 完整 | 无 | **缺失** |
| Nextcloud Talk | 完整 | 无 | **缺失** |
| Nostr | 完整 | 无 | **缺失** |
| Synology Chat | 完整 | 无 | **缺失** |
| Twitch | 完整 | 无 | **缺失** |
| Zalo | 完整 | 无 | **缺失** |
| WebChat | 完整 | 基础 Web UI | **中** |
| ChannelManager 框架 | 完整插件体系 | 基础接口定义 | **中** |

### 2.3 多智能体系统

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| 多代理路由 | 基于 channel/account/peer 的路由 | AgentRouter + ActorSystem | 相当 |
| 代理隔离 | Workspace 隔离 + 独立 Session | AgentPool | 相当 |
| DAG 任务编排 | 无（线性代理循环） | DynamicDAGBuilder + TaskPlanner | **EvoClaw 独有** |
| 代理委托架构 | Delegate + Presence 系统 | 无 | **缺失** |
| 代理生命周期 | 基础状态管理 | AgentLifecycle（完整 5 态） | EvoClaw 更优 |
| 代理池管理 | 单实例为主 | AgentPool（多实例池化） | EvoClaw 更优 |
| 队列管理 | Command Queue + Steering Queue | QueueManager | 相当 |
| 并行处理 | 有限 | TaskOrchestrator（并行编排） | EvoClaw 更优 |

---

## 三、工具与技能系统

### 3.1 核心工具

| 工具 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| 文件系统 (read/write/edit) | 有 | 有 | 相当 |
| 进程管理 | 有 | ProcessManager | 相当 |
| 浏览器控制 | Playwright-based | PlaywrightBrowser | 相当 |
| 消息发送 (message_send) | 有（多通道） | 有（WebChat） | **中** |
| Canvas | Live Canvas + A2UI | 无 | **缺失** |
| Nodes 控制 | 完整设备控制 | 无 | **缺失** |
| Discord/Slack 动作 | Discord/Slack 专用工具 | 无 | **缺失** |
| Cron 管理 | 完整 cron 工具 | CronScheduler | 相当 |
| Session 工具 | sessions_list/spawn/send/history | 部分（基础 session） | **中** |
| Email 工具 | 有 | EmailClient | 相当 |
| Voice/Talk | Voice Wake + Talk Mode | 无 | **缺失** |

### 3.2 技能系统

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| SKILL.md 规范 | 有 | 有 | 相当 |
| 技能注册表 | 有 | SkillRegistry | 相当 |
| 技能自动发现 | 文件扫描 | AutoSkillManager (TF-IDF) | EvoClaw 更优 |
| 技能沙盒 | Docker/SSH/OpenShell | SkillSandbox (进程隔离) | **中** |
| 技能市场 | ClawHub (clawhub.ai) | 无 | **缺失** |
| 技能分发 | 基础 | SkillDispatcher + SkillOrchestrator | EvoClaw 更优 |
| 内置技能数量 | 大量（extensions/skills） | 10 个 | 数量差距 |
| TF-IDF 技能匹配 | 无 | 有 | EvoClaw 独有 |
| 任务分类 + 意图识别 | 无 | TaskClassifier（11 意图 87 示例） | EvoClaw 独有 |

---

## 四、会话与记忆系统

### 4.1 会话管理

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| 会话作用域 (dmScope) | main/per-peer/per-channel-peer | SessionManager（基础） | **中** |
| 线程绑定 (Thread Bindings) | 有（idle/max-age） | 无 | **缺失** |
| 会话重置策略 | 支持（daily/by-hour/idle） | 基础重置 | **中** |
| 会话工具 | 完整（list/history/send/spawn） | 基础 | **中** |
| 会话持久化 | sessions.json | Memory Hub | 相当 |
| 上下文配置/切换 | `/focus`, `/unfocus`, `/agents` | 无 | **缺失** |

### 4.2 记忆系统

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| 短期记忆 | 会话内上下文 | ShortTermMemory | 相当 |
| 长期记忆 | AGENTS.md 持久化 | LongTermMemory + 持久化存储 | **EvoClaw 更优** |
| 语义记忆 | 无 | SemanticMemory | **EvoClaw 独有** |
| 向量记忆 | LanceDB 插件 | VectorMemory | 相当 |
| 知识图谱记忆 | 无 | KnowledgeGraphMemory | **EvoClaw 独有** |
| Memory Hub 统一接口 | 无 | MemoryHost + MemoryHostSDK | **EvoClaw 独有** |
| 经验分析 | 无 | ExperienceAnalyzer | **EvoClaw 独有** |

### 4.3 上下文压缩

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| 上下文压缩 | 有（Compaction） | CompactionManager（多策略） | **EvoClaw 更优** |
| 压缩策略 | 基础 | Token-based + Heuristic 多策略 | EvoClaw 更优 |
| 压缩触发 | `/compact` 命令 | 自动 + 手动 | 相当 |

---

## 五、LLM 模型支持

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| Provider 插件体系 | 完整 SDK（amazon-bedrock, anthropic-vertex 等） | ModelCatalog（基础） | **大** |
| Model Failover | 完整（主模型 + fallback 链 + auth rotation） | 基础错误分类 | **大** |
| Dynamic Model Discovery | 有（provider/* 按提供商过滤） | 无 | **缺失** |
| Model Aliases | 有（alias 系统） | 无 | 缺失 |
| LM Studio 集成 | 有（plugin-sdk/lmstudio） | 无 | 缺失 |
| Self-Hosted Providers | 支持（plugin-sdk） | 部分 | **中** |
| `/model` 运行时切换 | 有 | 无 | 缺失 |
| 图片尺寸控制 | `imageMaxDimensionPx` | 无 | 缺失 |
| Thinking/Trace 模式 | `/think <level>`, `/trace on|off` | 无 | 缺失 |

---

## 六、自动化系统

### 6.1 Cron 任务

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| Cron 调度 | 有（时间 + 事件 + 条件） | CronScheduler + ScheduleManager | **EvoClaw 更优** |
| Run Log | cron/runs/<jobId>.jsonl | 无 | **缺失** |
| 并发控制 | maxConcurrentRuns | 有 | 相当 |
| Session Retention | prune 已完成的 cron sessions | 无 | 缺失 |

### 6.2 Webhook

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| HTTP Webhook 端点 | 完整 hooks 系统 | 无 | **缺失** |
| Hook 路由匹配 | 路径匹配 + action 映射 | 无 | **缺失** |
| Gmail Pub/Sub | 有 | 无 | 缺失 |
| Dedicated Token Auth | header-only auth | 无 | 缺失 |

### 6.3 Heartbeat

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| 定期检查 | 有（30m/2h 等） | HeartbeatSystem | 相当 |
| Target 通道 | last/none/<channel-id> | 基础 | **中** |
| Direct Policy | allow/block | 无 | 缺失 |

---

## 七、安全系统

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| DM Pairing | 完整配对码 + allowlist | 无（WebChat 单用户） | **大** |
| Sandbox | Docker/SSH/OpenShell 多后端 | SkillSandbox（进程级） | **大** |
| SSRF 保护 | ssrf-policy + ssrf-runtime | 无 | **缺失** |
| 设备信任 | 设备身份 + challenge 签名 | JWT Token | **中** |
| RBAC | 基础 allowlist | RBACManager | EvoClaw 更优 |
| 审计日志 | 有 | AuditCenter | 相当 |
| Rate Limiting | 有 | RateLimiter | 相当 |
| 安全治理 | Security Guide | SecurityGovernor | EvoClaw 更优 |
| Formal Verification | MITRE ATLAS 威胁模型 | 无 | **缺失** |
| 自愈系统 | 无 | SelfHealingMonitor | **EvoClaw 独有** |
| 异常检测 | 基础 | AnomalyDetector | EvoClaw 更优 |

---

## 八、平台应用与节点

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| macOS App | 完整（菜单栏 + 语音 + Canvas + SSH） | 无 | **缺失** |
| iOS Node | 完整（语音触发 + Canvas + 推送） | 无 | **缺失** |
| Android Node | 完整（Connect/Chat/Voice + Camera + Screen） | 无 | **缺失** |
| Web UI | Control UI + Dashboard + WebChat | Web Dashboard（React） | **中** |
| TUI | 终端 UI | 无 | **缺失** |
| CLI | openclaw CLI（onboard/config/doctor/update） | 基础 CLI | **大** |
| Desktop 守护进程 | launchd/systemd daemon | 无 | **缺失** |
| Push Notifications | APNs + Relay | 无 | **缺失** |
| Canvas 系统 | Agent 驱动的可视化工作区 | 无 | **缺失** |

---

## 九、自我进化系统

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| 进化引擎 | 无 | EvolutionEngine | **EvoClaw 独有** |
| 需求挖掘 | 无 | NeedMiner | **EvoClaw 独有** |
| 进化提议 | 无 | EvolutionProposer | **EvoClaw 独有** |
| 强化学习反馈 | 无 | ReinforcementFeedback | **EvoClaw 独有** |
| 学习日志 | 无 | LearningJournal | **EvoClaw 独有** |
| 进度报告 | 无 | ProgressReporter | **EvoClaw 独有** |

---

## 十、运维与基础设施

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| Docker 支持 | Dockerfile + docker-compose | 无 | **缺失** |
| CI/CD | GitHub Actions 完整流水线 | 无 | **缺失** |
| 自动更新 | openclaw update + appcast | 无 | **缺失** |
| 更新通道 | stable/beta/dev 多通道 | 无 | **缺失** |
| Nix 支持 | nix-openclaw | 无 | 缺失 |
| Fly.io 部署 | fly.toml | 无 | 缺失 |
| Render 部署 | render.yaml | 无 | 缺失 |
| 守护进程 | launchd/systemd + --install-daemon | 无 | **缺失** |
| 日志系统 | 结构化日志 | Logger | 相当 |
| Health Check | 完整 health/healthz/live/ready | 完整 | 相当 |
| Config Doctor | `openclaw doctor` 诊断工具 | 无 | **缺失** |
| Last-Known-Good Config | 配置恢复机制 | 无 | **缺失** |
| Onboarding CLI | `openclaw onboard` 交互式向导 | 无 | **缺失** |
| Dev Hot Reload | gateway:watch | 无 | **缺失** |
| 报告生成 | 无 | ReportGenerator | **EvoClaw 独有** |

---

## 十一、消息与通讯系统

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| Streaming | 完整 streaming + chunking | 基础 | **中** |
| Progress Drafts | progress-drafts 机制 | 无 | 缺失 |
| Retry Policy | retry policy + idempotency | 基础 | **中** |
| Reply Dedup | reply-dedupe | 无 | 缺失 |
| 媒体处理 | media-runtime + media-store + media-mime | 基础 | **中** |
| Inbound Envelope | inbound-envelope 标准化 | 无 | 缺失 |
| Outbound Send | outbound-runtime | 基础 | **中** |
| 消息生命周期 | 完整生命周期（message-lifecycle-refactor） | 基础 | **大** |
| Reply Reference | reply-reference 引用链 | 无 | 缺失 |

---

## 十二、测试与质量保证

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| 单元测试 | Vitest | 无 | **缺失** |
| E2E 测试 | Playwright + Mocked Control UI | 无 | **缺失** |
| QA Lab | qa-lab + qa-channel + qa-matrix | 无 | **缺失** |
| BA 工具追踪 | Bus tool trace | 无 | 缺失 |
| 威胁建模 | MITRE ATLAS + Formal Verification | 无 | **缺失** |
| 版本管理 | Changelog + release automation | 无 | **缺失** |

---

## 十三、开发者体验

| 功能 | OpenClaw | EvoClaw | 差距 |
|------|----------|---------|------|
| Plugin SDK | 完整 plugin-sdk（20+ 导出子路径） | PluginSystem（基础钩子） | **大** |
| Provider SDK | provider-setup + self-hosted | 无 | **缺失** |
| Channel SDK | channel-runtime + channel-setup | 基础接口 | **大** |
| Setup Wizard | `setup` 命令 + `onboard` | start.bat | **中** |
| Config RPC | `config.set/get/unset` | 基础 ConfigManager | **大** |
| Doctor 诊断 | `openclaw doctor --fix` | 无 | **缺失** |
| Config 严格验证 | TypeBox Schema + 启动拒绝 | ConfigSchema（基础） | **大** |
| Pre-commit hooks | pre-commit-config.yaml | 无 | 缺失 |

---

## 十四、功能差距汇总

### 标记说明
- 🔴 **P0 - 核心缺失**：严重影响产品竞争力
- 🟠 **P1 - 重要缺失**：影响用户体验和功能完整性
- 🟡 **P2 - 次要缺失**：锦上添花
- 🟢 **独有优势**：EvoClaw 独有的差异化功能

### 🔴 P0 核心缺失（15 项）

1. **多通道集成**（25+ 通道完全缺失）
2. **Plugin SDK**（无插件扩展体系）
3. **WebSocket 协议标准化**（无类型化 RPC 协议）
4. **Device Pairing 安全机制**（无设备信任体系）
5. **Sandbox 沙盒**（仅进程级，缺 Docker/SSH）
6. **CI/CD 流水线**（完全缺失）
7. **测试基础设施**（完全缺失）
8. **Docker 部署**（无 Dockerfile）
9. **CLI 命令行工具**（仅基础，缺 onboard/config/doctor/update）
10. **守护进程安装**（无 launchd/systemd daemon）
11. **配置热重载与严格验证**（无 Schema 验证 + 热重载）
12. **Webhook 系统**（完全缺失）
13. **Canvas 可视化工作区**（完全缺失）
14. **Model Failover**（无模型回退机制）
15. **Plugin Provider 体系**（无提供商插件）

### 🟠 P1 重要缺失（12 项）

16. **会话线程绑定与重置策略** 
17. **Reply Dedup + Retry Policy**
18. **消息生命周期管理**
19. **Cron Run Log** 
20. **Heartbeat Target 通道**
21. **Config Doctor 诊断工具**
22. **Config RPC 运行时配置**
23. **Streaming 流式响应增强**
24. **图片/媒体尺寸控制**
25. **运行时模型切换**
26. **Onboarding Setup Wizard**
27. **Dev Hot Reload**

### 🟡 P2 次要缺失（10 项）

28. TUI 终端界面
29. Nix 打包
30. 自动更新通道
31. Fly.io/Render 部署模板
32. Pre-commit hooks
33. 威胁建模文档
34. Progress Drafts
35. Thinking/Trace 调试模式
36. Plugin Provider 市场（ClawHub 等价物）
37. Context Focus 切换命令

### 🟢 EvoClaw 独有优势（8 项）

1. **DAG 任务编排 + TaskPlanner**（OpenClaw 无此能力）
2. **进化引擎**（EvolutionEngine + NeedMiner + LearningJournal）
3. **多维记忆系统**（语义记忆 + 知识图谱记忆）
4. **MCP 协议支持**
5. **TF-IDF 技能自动匹配**
6. **自愈系统**（SelfHealingMonitor）
7. **任务分类器 + 意图识别**
8. **报告生成器**

---

## 十五、功能补全方案

### 阶段一：基础设施补齐（预估 4-6 周）

| 项目 | 技术路径 | 优先级 |
|------|---------|--------|
| **CLI 工具开发** | 新建 `apps/cli/`，基于 Commander.js，实现 `evoclaw onboard/config/doctor/update/start` | P0 |
| **CI/CD 搭建** | GitHub Actions workflow: lint → test → build → docker push | P0 |
| **测试基础设施** | 引入 Vitest + Playwright，为 core/gateway/agent 包编写测试 | P0 |
| **Docker 部署** | 编写 Dockerfile + docker-compose.yml，支持一键部署 | P0 |
| **Plugin SDK** | 参考 OpenClaw plugin-sdk，新建 `packages/plugin-sdk/`，提供标准扩展接口 | P0 |
| **配置系统升级** | 引入 TypeBox/JSON Schema 严格验证 + 文件监听热重载 + Doctor 诊断 | P0 |

### 阶段二：核心功能对齐（预估 6-8 周）

| 项目 | 技术路径 | 优先级 |
|------|---------|--------|
| **WebSocket 协议标准化** | 基于 TypeBox 定义请求/响应/事件 Schema，实现类型化 RPC | P0 |
| **多通道集成（第一批）** | Telegram (grammY)、Discord (discord.js)、Slack (@slack/bolt) | P0 |
| **Model Failover** | 增强 AgentModelExecutor，支持 primary + fallbacks chain + auth rotation | P0 |
| **Sandbox 沙盒** | 集成 Docker sandbox（参考 OpenClaw sandbox），扩展现有 SkillSandbox | P0 |
| **Webhook 系统** | 新建 `packages/webhook/`，实现 HTTP endpoint + 路由匹配 + token auth | P0 |
| **Device Pairing** | 实现设备 ID + challenge 签名 + 一键配对（替换纯 JWT） | P1 |

### 阶段三：体验与扩展（预估 4-6 周）

| 项目 | 技术路径 | 优先级 |
|------|---------|--------|
| **Canvas 可视化** | 参考 OpenClaw A2UI，新建 `/__evoclaw__/canvas/` 路径，Agent 可编辑 HTML | P1 |
| **TUI 终端界面** | 基于 ink.js (React for CLI) 构建终端交互界面 | P2 |
| **守护进程安装** | Windows: nssm/Windows Service, macOS: launchd plist, Linux: systemd | P1 |
| **多通道集成（第二批）** | WhatsApp (Baileys)、WeChat、Feishu、QQ、Matrix | P1 |
| **Skill Marketplace** | 新建 `clawhub` 等价物网站，支持技能发布/搜索/安装 | P2 |
| **自动更新** | 基于 GitHub Releases + appcast.xml 实现更新检测与自动安装 | P2 |

### 阶段四：进阶能力（预估 4-6 周）

| 项目 | 技术路径 | 优先级 |
|------|---------|--------|
| **macOS App** | SwiftUI 菜单栏 App + WebSocket 连接 Gateway | P2 |
| **iOS/Android Node** | React Native 节点 App + WS 通信 + Camera/Voice | P2 |
| **Provider 插件生态** | 参考 OpenAI/anthropic-vertex/amazon-bedrock 实现 | P1 |
| **Formal Verification** | MITRE ATLAS 适配 + 威胁模型文档 | P2 |
| **Progress Drafts** | 流式进度报告机制 | P2 |

### 资源需求评估

| 阶段 | 人月 | 关键技能 |
|------|------|---------|
| 阶段一 | 3-4 | TypeScript, DevOps, Docker, Testing |
| 阶段二 | 4-6 | TypeScript, WebSocket, Baileys/grammY, Docker |
| 阶段三 | 3-4 | React, SwiftUI, React Native, Tailscale |
| 阶段四 | 4-6 | SwiftUI/Kotlin, Security, Provider APIs |
| **总计** | **14-20 人月** | |

### 时间规划

```
Month 1-2:  ████████████ 阶段一：基础设施补齐
Month 3-4:  ████████████ 阶段二：核心功能对齐
Month 5-6:  ████████████ 阶段三：体验与扩展
Month 7-8:  ████████████ 阶段四：进阶能力
```

---

## 十六、结论

EvoClaw 在**自我进化**（Evolution Engine）、**多维记忆**（Semantic/Knowledge Graph）、**任务编排**（DAG/Planner）和 **MCP 协议**方面具有独特优势，体现了创新的架构设计思路。

但在**产品化**方面与 OpenClaw 差距显著：
- 缺少 25+ 通道集成和 Plugin SDK 生态
- 无 CI/CD、测试、Docker 等工程基础设施
- 无多平台应用（macOS/iOS/Android）
- 无完整的 CLI 工具链和安全配对机制

建议按四阶段方案逐步补全，优先完成基础设施和核心功能（阶段一+二），再扩展到体验和进阶能力。